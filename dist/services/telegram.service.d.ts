import TelegramBot from 'node-telegram-bot-api';
import { UserService } from './user.service';
import { IApartment } from 'src/interfaces/IApartment';
export declare class TelegramService {
    private userService;
    private readonly logger;
    private readonly bot;
    private chatId;
    private currentState;
    private user;
    private roomsFilter;
    private districtsFilter;
    constructor(userService: UserService);
    startListenCommands(): Promise<void>;
    startListenCallbacks(): Promise<void>;
    saveMaxPriceFilter(maxPrice: number): Promise<TelegramBot.Message>;
    saveFloorFilter(minFloorFilter: number, maxFloorFilter: number): Promise<TelegramBot.Message>;
    sendApartment(chatId: number, apartment: IApartment): Promise<void>;
    sendMainMenu(): Promise<TelegramBot.Message>;
    sendSearchMenu(): Promise<TelegramBot.Message>;
    sendSupport(): Promise<TelegramBot.Message>;
    sendAbout(): Promise<TelegramBot.Message>;
    sendFiltersMenu(): Promise<TelegramBot.Message>;
    sendSuccessfullyUpdate(): Promise<TelegramBot.Message>;
    sendSearchUpdate(isSearchActive: boolean): Promise<TelegramBot.Message>;
    sendRoomsFilter(): Promise<TelegramBot.Message>;
    sendDistrictsFilter(): Promise<TelegramBot.Message>;
    sendMaxPriceFilter(): Promise<TelegramBot.Message>;
    sendFloorFilter(): Promise<TelegramBot.Message>;
    private getCurrentFloorFilter;
    private getCurrentMaxPriceFilter;
    private getCurrentRoomsFilter;
    private getCurrentDistrictsFilter;
}
